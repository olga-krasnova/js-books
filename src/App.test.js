describe('App Router test', () => {
  test('should show HomePage on main page', () => {
    // Arrange

    // Act

    // Assert
  });

  test('should show CartPage on /cart', () => {
    // Arrange

    // Act

    // Assert
  });
});
